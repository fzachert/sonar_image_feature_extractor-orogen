PROJECT(sonar_image_feature_extractor)
cmake_minimum_required(VERSION 2.6)

SET (CMAKE_MODULE_PATH "${CMAKE_SOURCE_DIR}/.orogen/config")
INCLUDE(sonar_image_feature_extractorBase)

# FIND_PACKAGE(KDL)
# FIND_PACKAGE(OCL)

